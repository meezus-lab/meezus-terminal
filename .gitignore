# ─── Python ───────────────────────────────────────────────────────────────
venv/
__pycache__/
*.pyc
*.pyo
.Python

# ─── Kronos forecast model ──────────────────────────────────────────────────
# Auto-cloned + weights auto-downloaded by `python3 forecast_setup.py`.
# Not committed (nested git repo + large training data); the setup script
# recreates it from the upstream repo + HuggingFace.
kronos_src/

# ─── macOS ──────────────────────────────────────────────────────────────────
.DS_Store

# ─── Local launch agents / logs ─────────────────────────────────────────────
*.log
com.meezus.dashboard.plist

# ─── Generated runtime data ─────────────────────────────────────────────────
# Rolling Crypto-Pulse sentiment history; rebuilt on each machine as it runs.
pulse_history.json
